# Speech Therapy ML Server — Python dependencies
# Tested on Python 3.12, Raspberry Pi OS (64-bit)
#
# Install:
#   pip install -r requirements.txt
#
# On RPi Zero 2W — librosa takes ~4 min to install, be patient.
# If numpy/scipy conflict, install them first:
#   pip install numpy scipy && pip install -r requirements.txt

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.9
sqlalchemy==2.0.35

librosa==0.10.2
soundfile==0.12.1
sounddevice==0.5.1
scipy==1.14.1
numpy==2.1.1
rich==13.8.1
requests==2.32.3
